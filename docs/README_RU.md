# claude-recall

[English](../README.md) | [Chinese / 中文](README_ZH.md)

Сотни твоих прошлых сессий Claude Code перестают быть кладбищем: говоришь «найди
сессию, где я выгружал сделки этого клиента» — и через секунду у тебя проект, метод,
ссылка на готовый артефакт и три способа этим воспользоваться.

**Как это работает:** каждую завершённую сессию маленькая LLM (haiku) сжимает в
recap-карточку; все карточки сворачиваются в один INDEX.md — строка на сессию,
грепается мгновенно. Это единая точка касания для тебя и всех твоих агентов.
Скилл в комплекте учит любую сессию Claude Code искать по индексу и возобновлять
прошлую работу.

**Без зависимостей** — один файл Python, без API-ключей (работает через твою
существующую авторизацию `claude`).

```
СЕССИЯ ЗАВЕРШИЛАСЬ → SessionEnd-хук            → recap (+ handoff, если включён)
РЕКАПЕР (haiku)    → сжимает сессию в карточку → ~/.claude/session-recaps/<id>.md
ИНДЕКС             → строка на сессию          → INDEX.md  ← единая точка касания
ОРКЕСТРАТОР (скилл)→ find → show → finish | handoff | spawn

ОПЦИОНАЛЬНО:       → PreCompact-хук пишет свежий HANDOFF (задача / сделано /
ПЕТЛЯ HANDOFF        в полёте / дальше / грабли) → SessionStart вливает его
                     обратно — сессия просыпается, зная, на чём остановилась
```

Recap-карточка — десять строк YAML:

```yaml
id: c2631a96-...
project: ~/Documents/Cursor/my-crm
task: Выгрузка сделок уволенного логиста Ивановой в Google Sheet для РОПа
status: done
method: скилл export-dismissed + Sheets API
entities: [Иванова, Bitrix]
artifacts: [https://docs.google.com/spreadsheets/d/...]
next: null
```

## Три режима

| режим | ты говоришь | что происходит |
|-------|-------------|----------------|
| **finish** | «сделай ещё раз ту выгрузку, по клиенту X» | Claude находит прошлую сессию, достаёт проект + метод, повторяет задачу прямо тут |
| **handoff** | «дай команду» | `recall cmd <id>` → `cd "<проект>" && claude --resume <id>` |
| **spawn** | «открой ту сессию» | `recall open <id>` — новое окно терминала с восстановленной сессией |

Если кандидатов несколько — оркестратор не гадает, а показывает селектор:
папка проекта + дата + контекст задачи на каждого, плюс вариант «копнуть глубже».

## Сессии называют себя сами

Каждый recap (и каждый handoff, если петля включена) заодно даёт сессии имя
**`папка/проект · задача`** — тем же механизмом, что и `/rename`, — и пикер
`claude --resume` перестаёт быть стеной из «no name». Имя обновляется по мере
эволюции задачи. Имена, заданные руками, никогда не перезаписываются; задать
явно: `recall rename <id8> <имя>`; выключить: `RECALL_AUTONAME=0`.

## Статус-бар: у каждой вкладки есть адрес

Установщик ставит ещё и статус-бар Claude Code, заточенный под рулёжку *флотом*
сессий. Каждый бар выглядит так:

```
• claude-recall [s034] | 5h:63% 2h0m 7d:18% 2d7h | ctx:45% | e:xh | opus4.8 1M
```

- **`[s034]`** — главная фича: **адрес** вкладки. Это её iTerm-tty. Говоришь «иди в
  s034» — и оркестратор впрыскивает контекст прямо в эту вкладку
  (`osascript … write text`), не разыскивая нужное окно. tty добывается проходом
  **вверх** по дереву процессов от самого скрипта (статус-бар крутится на пайпе без
  своего tty) до процесса `claude`, который владеет реальным терминалом; если ничего
  не нашлось — fallback на первые 8 символов session_id.
- **`5h:63% 2h0m` / `7d:18% 2d7h`** — твои лимиты Pro/Max в **процентах остатка** плюс
  время до резета, цветовая шкала зелёный / жёлтый / красный (нужен Claude Code
  ≥ 2.1.80).
- **`ctx:45%`** — заполнение контекста. **`e:xh`** — уровень effort из
  `settings.json` `.effortLevel` (`xh`/`hi`/`md`/`lo`). **`opus4.8 1M`** — модель,
  компактно, с суффиксом `1M` для 1M-варианта.

Так флот вкладок становится поимённо адресуемым: глянул на бары, выбрал вкладку по её
`[s0NN]` — и оркестратор рулит именно той сессией.

## Установка

Вставь в Claude Code:

> Склонируй github.com/rocketmandrey/claude-recall и запусти ./install.sh. Спроси
> меня, включать ли опциональную handoff-петлю, покажи, что он изменил, потом спроси,
> запускать ли `recall backfill` (LLM-вызов на каждую прошлую сессию, ~20–40 минут
> на пару сотен).

Или вручную:

```bash
git clone https://github.com/rocketmandrey/claude-recall.git && cd claude-recall
./install.sh              # CLI + скилл + SessionEnd-хук + permissions
recall backfill           # опционально: проиндексировать прошлые сессии
```

`install.sh` мержит в `~/.claude/settings.json` (никогда не перезаписывает):
SessionEnd-хук, permissions `Bash(recall *)` и команду `statusLine` — чтобы агенты
звали recall в любой новой сессии без промптов. Дальше каждая завершённая сессия
(≥15 событий) индексируется сама. Мерж статус-бара идемпотентен и **никогда не
затирает чужой**: если `statusLine` уже указывает на другую команду, установщик
оставляет твою и просто печатает, как переключиться.

**Handoff-петля — opt-in**: установщик её предлагает, но не навязывает. Перед
каждым компактом контекста маленькая LLM пишет карточку состояния, которая
вливается обратно после компакта/резюма — сессия просыпается, зная, на чём
остановилась. Цена: один вызов haiku на компакт. Флаги: `./install.sh
--with-handoff` / `--no-handoff` (без флага установщик спросит; при переустановке
прежний выбор сохраняется). Проверка: `recall doctor`.

## Использование

Просто скажи в Claude Code:

- «найди сессию, где я выгружал сделки»
- «мы это уже делали — сделай ещё раз по клиенту X»
- «открой сессию, где мы пилили дашборд»
- «что у меня висит недоделанного?»
- «забудь ту сессию» (удаление из индекса — с подтверждением)

### Примеры

**Повторить прошлую задачу:**
> Сделай ещё раз ту выгрузку по уволенным, но по постоянным Светланы

**Открыть прошлую сессию в новом окне:**
> Открой сессию, где мы настраивали деплой — прям окошком

**Спросить, что было решено:**
> Что мы тогда решили насчёт схемы деплоя?

## Структура репозитория

```
claude-recall/
├── bin/recall                  ← весь продукт: один файл Python, ноль зависимостей
├── bin/statusline-command.sh   ← статус-бар флота: проект [s0NN] | лимиты | ctx | модель
├── skill/SKILL.md              ← скилл session-orchestrator для Claude Code
├── install.sh                  ← установщик: CLI + скилл + статус-бар + хук + permissions
└── docs/                       ← переводы README (RU, ZH)
```

## CLI

| команда | что делает |
|---------|------------|
| `recall find <запрос...>` | поиск по индексу (без регистра, OR; ищи по ОСНОВЕ слова: «Ивановой» → `иванов`) |
| `recall grep <запрос...>` | полнотекст по сырым транскриптам (fallback, когда индекс молчит) |
| `recall show <id8>` | карточка сессии |
| `recall cmd <id8>` | команда возобновления (handoff) |
| `recall open <id8> [--fast] [--tab]` | открыть сессию в новом окне терминала (spawn); если handoff-карточки нет — сначала сгенерит её (`--fast` — пропустить); `--tab` — открыть табом |
| `recall remove <id8>` | забыть сессию: карточка + строка индекса + tombstone |
| `recall rename <id8> <имя...>` | задать имя сессии руками (автонейминг его больше не тронет) |
| `recall handoff <file.jsonl>` | записать handoff-карточку (текущее состояние) сессии |
| `recall handoff --all` | переобход: карточки недавним сессиям без них `[--days N] [--jobs N]` |
| `recall backfill` | проиндексировать старые сессии `[--days N] [--min-events N] [--jobs N]` |
| `recall index` | статистика индекса |
| `recall doctor` | проверка установки |

Env: `RECALL_DATA` (по умолчанию `~/.claude/session-recaps`), `RECALL_MODEL`
(по умолчанию haiku), `RECALL_TERMINAL` (`Terminal`\|`iTerm`), `RECALL_MIN_EVENTS` (15),
`RECALL_HANDOFF_DAYS` (30 — срок хранения handoff'ов),
`RECALL_AUTONAME` (1 — автоимена сессий; `0` — выключить),
`RECALL_TAB` (`1` — `recall open` по умолчанию открывает табы; разово окном: `--window`).

## Требования и заметки

- macOS (`recall open` через AppleScript; PR для Linux приветствуются), python3 ≥ 3.9,
  залогиненный `claude` CLI.
- **Локально**: карточки и индекс не покидают машину. Но это выжимка твоих сессий —
  не коммить и не шарь папку данных.
- Рекапы идут через `claude -p` — твоя существующая авторизация, API-ключ не нужен.
  Сессии-воркеры помечены и сами никогда не индексируются.
- `recall remove` ставит tombstone — хук/backfill сессию не воскресят
  (вернуть: `recall recap <transcript> --force`).
- Handoff-карточки (если петля включена) весят пару КБ и автоматически чистятся
  через 30 дней; долгая память живёт в recap-карточках. Сессия, открытая через
  `recall open`, получает свой handoff автоматически — просыпается, зная следующие
  шаги. Старым сессиям без карточки `recall open` сгенерит её на месте (~30 сек;
  `--fast` — пропустить), переобход всех недавних: `recall handoff --all --days 14`.
- Если индекс молчит, `recall grep` ищет полнотекстом по сырым транскриптам
  в `~/.claude/projects/` — никаких дополнительных инструментов.
- Табы: в iTerm `recall open --tab` работает нативно и надёжно, и каждый таб
  **красится по проекту** (iTerm OSC-6) — в вертикальном таб-баре сессии одного
  проекта сгруппированы цветной полосой (таб-бар слева: iTerm → Settings →
  Appearance → Tab bar location). У Terminal.app API табов нет — recall эмулирует
  ⌘T через System Events (best-effort: нужно разрешение Accessibility, и даже с
  ним скриптовый ⌘T может не сработать из-за фокуса). recall проверяет, что таб
  реально появился, иначе открывает окно — так команда никогда не впечатывается
  в занятый таб. Нужны гарантированные цветные табы — ставь iTerm.

## Благодарности

recall вырос из ежедневного использования [codbash](https://github.com/vakovalskii/codbash)
Валерия Ковальского — дашборда с полнотекстовым поиском по сессиям AI-кодинга. Кода
codbash в recall нет и зависимости от него нет, но сама идея относиться к прошлым
сессиям как к searchable-активу — из жизни с codbash, а `recall grep` — намеренно
минимальный поклон тому, что codbash делает по-взрослому (веб-дашборд, синк сессий
разных агентов). За полным опытом — ставь codbash. Респект, Валера!

## Лицензия

MIT
